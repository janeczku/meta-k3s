#!/bin/sh

### BEGIN INIT INFO
# Provides:          k3s
# Required-Start:    $local_fs $remote_fs $network
# Required-Stop:     $local_fs $remote_fs $network
# Default-Start:     2 3 4 5
# Default-Stop:      0 1 6
# Short-Description: Lightweight Kubernetes
### END INIT INFO

PATH=/usr/local/sbin:/usr/local/bin:/sbin:/bin:/usr/sbin:/usr/bin
DAEMON=@BINDIR@/k3s
DAEMON_OPTS=
NAME=k3s
DESC=k3s
PID="/var/run/k3s.pid"

test -x $DAEMON || exit 0

# These values may be replaced by those from /etc/default/k3s
K3S_ARGS="server --disable-agent"

# Include k3s environment file if available
if [ -f /etc/default/k3s ] ; then
        . /etc/default/k3s
fi

set -e

case "$1" in
  start)
        echo -n "Starting $DESC: "
        rm -f /tmp/k3s.*
        start-stop-daemon --start --quiet --pidfile "${PID}" --make-pidfile \
                --name $NAME --exec $DAEMON -- $K3S_ARGS
        echo "$NAME."
        ;;
  stop)
        echo -n "Stopping $DESC: "
        start-stop-daemon --stop --quiet --pidfile "${PID}" --retry=TERM/30/KILL/5 \
                --name $NAME
        echo "$NAME."
        ;;
  restart|force-reload)
        echo -n "Restarting $DESC: "
        start-stop-daemon --stop --quiet --pidfile "${PID}" --retry=TERM/30/KILL/5 \
                --name $NAME
        sleep 1
        rm -f /tmp/k3s.*
        start-stop-daemon --start --quiet --pidfile "${PID}" --make-pidfile \
                --name $NAME --exec $DAEMON -- $K3S_ARGS
        echo "$NAME."
        ;;
  *)
        N=/etc/init.d/$NAME
        echo "Usage: $N {start|stop|restart|force-reload}" >&2
        exit 1
        ;;
esac

exit 0